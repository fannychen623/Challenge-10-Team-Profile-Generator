const Employee = require('../lib/Employee');

describe("Employee", () => {
  describe("Initialization", () => {
    it("should", () => {
      const obj = new Employee();

      expect().toEqual(true);
    });
  });
});
