const Intern = require('../lib/Intern');

describe("Intern", () => {
  describe("Initialization", () => {
    it("should", () => {
      const obj = new Intern();

      expect().toEqual(true);
    });
  });
});