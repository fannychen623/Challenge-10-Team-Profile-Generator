const Engineer = require('../lib/Engineer');

describe("Engineer", () => {
  describe("Initialization", () => {
    it("should", () => {
      const obj = new Engineer();

      expect().toEqual(true);
    });
  });
});